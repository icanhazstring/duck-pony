MySQL:
  hostname: ${db_host}
  username: ${db_username}
  password: ${db_password}

Jira:
  hostname: ${host}
  username: ${username}
  password: ${password}

CleanBranch:
  pattern:  ${pattern}

PurgeService:
  pattern: ${instancePattern}
